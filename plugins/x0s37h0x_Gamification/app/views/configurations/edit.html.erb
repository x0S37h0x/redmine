<%= link_to 'Bearbeiten', edit_configurations_path %>
<%= form_for @config, url: configurations_path, method: :patch do |f| %>
  <!-- Formularfelder für Konfigurationswerte -->
  <%= f.label :exp_leicht, 'Experience für Leicht' %>
  <%= f.number_field :exp_leicht %>

  <%= f.label :exp_mittel, 'Experience für Mittel' %>
  <%= f.number_field :exp_mittel %>

  <%= f.label :exp_schwer, 'Experience für Schwer' %>
  <%= f.number_field :exp_schwer %>

  <%= f.submit 'Speichern' %>
<% end %>
